Place vehicle sounds here.

Name each `.wav` file after the vehicle (e.g. `car.wav`, `train.wav`,
`boat.wav`). These files will be played when selecting the corresponding
sound in the Vehicles category.