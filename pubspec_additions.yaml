# Paste in your pubspec.yaml

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  # firebase_core: ^3.6.0  # (daca folosesti Firebase, lasa activ si ruleaza `flutterfire configure`)

flutter:
  generate: true
