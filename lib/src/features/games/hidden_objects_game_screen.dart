import 'package:flutter/material.dart';

class HiddenObjectsGameScreen extends StatelessWidget {
  const HiddenObjectsGameScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Hidden Objects Game')),
      body: const Center(child: Text('Hidden Objects Game - Under Construction')),
    );
  }
}
