import 'package:flutter/material.dart';

class SortingAnimalsGameScreen extends StatelessWidget {
  const SortingAnimalsGameScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Sorting Animals Game')),
      body: const Center(child: Text('Sorting Animals Game - Under Construction')),
    );
  }
}
