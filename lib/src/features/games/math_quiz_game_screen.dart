import 'package:flutter/material.dart';

class MathQuizGameScreen extends StatelessWidget {
  const MathQuizGameScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Math Quiz')),
      body: const Center(child: Text('Math Quiz Game - Under Construction')),
    );
  }
}
