import 'package:flutter/material.dart';

class ShapesGameScreen extends StatelessWidget {
  const ShapesGameScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Shapes Game')),
      body: const Center(child: Text('Shapes Game - Under Construction')),
    );
  }
}
