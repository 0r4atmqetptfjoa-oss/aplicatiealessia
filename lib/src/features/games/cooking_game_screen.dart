import 'package:flutter/material.dart';

class CookingGameScreen extends StatelessWidget {
  const CookingGameScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Cooking Game')),
      body: const Center(child: Text('Cooking Game - Under Construction')),
    );
  }
}
