import 'package:flutter/material.dart';

class InstrumentsGameScreen extends StatelessWidget {
  const InstrumentsGameScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Instruments Game')),
      body: const Center(child: Text('Instruments Game - Under Construction')),
    );
  }
}
